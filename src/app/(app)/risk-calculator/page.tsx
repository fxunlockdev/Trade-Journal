import { RiskCalculatorClient } from "./risk-calculator-client";

export const metadata = { title: "Risk Calculator | TRDR" };

export default function RiskCalculatorPage() {
  return <RiskCalculatorClient />;
}
