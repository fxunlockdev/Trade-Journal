/**
 * The bot's public handle, as users see it.
 *
 * One constant, imported by every card that tells a person "send this to
 * @X". It was typed out in three places, which agreed by luck; a bot rename or
 * a second deployment would have sent people to the wrong chat from one card
 * and not another.
 */
export const BOT_HANDLE = "@TradingJournalImagesBot";
