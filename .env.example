# Supabase
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# Server-only
SUPABASE_SERVICE_ROLE_KEY=
OPENAI_API_KEY=
# Optional: override the AI model without a code change (default gpt-5.4-mini).
# Set to a known-good model (e.g. gpt-4o-mini) to roll back instantly.
OPENAI_MODEL=
TELEGRAM_BOT_TOKEN=
TELEGRAM_CHAT_ID=
MT5_WEBHOOK_SECRET=

# Admin Bootstrap (server-only)
BOOTSTRAP_SECRET=
